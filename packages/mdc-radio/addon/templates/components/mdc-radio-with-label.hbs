{{#mdc-form-field}}
  {{mdc-radio id=controlId
              name=name
              checked=checked
              disabled=disabled
              value=value
              required=required
              form=form}}
  <label for={{radioControlId}}>{{yield}}</label>
{{/mdc-form-field}}