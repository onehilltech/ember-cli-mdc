<div class="mdc-radio-buttons" {{on "change" this.changed}}>
  {{#if (has-block)}}
    {{yield (hash name=@name)}}
  {{else}}
    {{#each @buttons as |button|}}
      <MdcRadio id={{button.id}}
                name={{@name}}
                form={{@form}}
                required={{@required}}
                value={{button.value}}
                @label={{button.label}}
                disabled={{button.disabled}} />
    {{/each}}
  {{/if}}
</div>

