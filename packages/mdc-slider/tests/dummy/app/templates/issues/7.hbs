<Issue7Menu @open={{open}} as |menu|>
  <MdcSlider @min={{0}} @max={{100}} @value={{50}} @requestLayout={{menu.isOpenComplete}} />
</Issue7Menu>

{{#unless open}}
  <button {{action (mut open) true}}>open</button>
{{else}}
  <button {{action (mut open) false}}>close</button>
{{/unless}}
