import Component from '@ember/component';
import layout from '../templates/components/mdc-linear-progress-buffering-dots';

export default Component.extend({
  layout,

  classNames: ['mdc-linear-progress__buffering-dots']
});
