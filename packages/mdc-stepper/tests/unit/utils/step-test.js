import step from 'dummy/utils/step';
import { module, test } from 'qunit';

module('Unit | Utility | step', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = step();
    assert.ok(result);
  });
});
