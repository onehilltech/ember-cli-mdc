import Component from '@ember/component';
import layout from '../templates/components/mdc-card-action-buttons';

export default Component.extend({
  layout,

  classNames: ['mdc-card__action-buttons']
});
