{{mdc-checkbox id=id
               initialized=(action (mut input))
               checked=checked
               disabled=disabled
               required=required
               form=form}}
<label for={{id}}>{{yield}}</label>
