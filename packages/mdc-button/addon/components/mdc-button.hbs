<button class="mdc-button {{if @style (concat "mdc-button--" @style)}}"
        {{did-insert this.didInsert}}>
  <div class="mdc-button__ripple"></div>

  {{#if @icon}}
    <MdcButtonIcon @icon={{@icon}} />
  {{/if}}

  <span class="mdc-button__label">{{@label}}</span>
</button>
