{{#unless (has-block)}}
  {{mdc-button-ripple}}

  {{#if leadingIcon}}
    {{mdc-button-icon leadingIcon}}
  {{/if}}

  {{#mdc-button-label}}{{label}}{{/mdc-button-label}}

  {{#if trailingIcon}}
    {{mdc-button-icon trailingIcon}}
  {{/if}}
{{else}}
  {{yield}}
{{/unless}}
