<h5>Text Field</h5>

<Input @value={{value}} placeholder="Type here" />
<MdcTextfield @label="Label should float when value dynamically updated" @value={{value}} />

<h5>Text Area</h5>

<Textarea @value={{description}} placeholder="Type here" />
<MdcTextarea @label="Label should float when value dynamically updated" @value={{description}} />
