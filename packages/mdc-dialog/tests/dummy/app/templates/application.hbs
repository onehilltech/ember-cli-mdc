<h2 id="title">Dialogs</h2>

<p>{{#mdc-button style="unelevated" click=(action (mut showBasicDialog) "basic")}}Show Basic Dialog{{/mdc-button}}</p>
<p>{{#mdc-button style="unelevated" click=(action (mut showScrollableDialog) "scrollable")}}Show Scrollable Dialog{{/mdc-button}}</p>
<p>{{#mdc-button style="unelevated" click=(action (mut showEmphasizeDialog) "emphasize")}}Show Emphasize Dialog{{/mdc-button}}</p>

{{#mdc-dialog title="Use Google's location service?"
              accept=(action "accept")
              acceptButtonText="Accept"
              cancel=(action "cancel")
              cancelButtonText="Decline"
              show=showBasicDialog}}
  Let Google help apps determine location. This means sending anonymous
  location data to Google, even when no apps are running.
{{/mdc-dialog}}

{{#mdc-dialog title="Choose a Ringtone"
              scrollable=true
              accept=(action "accept")
              acceptButtonText="Accept"
              cancel=(action "cancel")
              cancelButtonText="Decline"
              show=showScrollableDialog}}
  <ul class="mdc-list">
    <li class="mdc-list-item">None</li>
    <li class="mdc-list-item">Callisto</li>
    <li class="mdc-list-item">Ganymede</li>
    <li class="mdc-list-item">Luna</li>
    <li class="mdc-list-item">Marimba</li>
    <li class="mdc-list-item">Schwifty</li>
    <li class="mdc-list-item">Callisto</li>
    <li class="mdc-list-item">Ganymede</li>
    <li class="mdc-list-item">Luna</li>
    <li class="mdc-list-item">Marimba</li>
    <li class="mdc-list-item">Schwifty</li>
  </ul>
{{/mdc-dialog}}

{{#mdc-dialog title="Use Google's location service?"
              accept=(action "accept")
              acceptEmphasize=true
              acceptButtonText="Accept"
              cancel=(action "cancel")
              cancelButtonText="Decline"
              show=showEmphasizeDialog}}
  Let Google help apps determine location. This means sending anonymous
  location data to Google, even when no apps are running.
{{/mdc-dialog}}