import Component from '@ember/component';
import layout from '../templates/components/mdc-dialog-actions';

export default Component.extend({
  layout,

  classNames: ['mdc-dialog__actions'],

  tagName: 'footer'
});
