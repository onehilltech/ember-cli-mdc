import Component from '@ember/component';
import layout from '../templates/components/mdc-dialog-footer';

export default Component.extend({
  layout,

  tagName: 'footer',

  classNames: ['mdc-dialog__footer']
});
