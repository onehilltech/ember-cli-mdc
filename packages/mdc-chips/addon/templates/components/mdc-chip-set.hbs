{{#unless (has-block)}}
  {{#each chips as |chip index|}}
    {{mdc-chip id=chip.id text=chip.text iconLeading=chip.iconLeading iconTrailing=chip.iconTrailing tabindex=index}}
  {{/each}}
{{else}}
  {{yield}}
{{/unless}}
