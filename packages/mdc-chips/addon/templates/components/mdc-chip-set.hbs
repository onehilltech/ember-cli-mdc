{{#unless (has-block)}}
  {{#each chips as |chip|}}
    {{mdc-chip id=chip.id text=chip.text iconLeading=chip.iconLeading iconTrailing=chip.iconTrailing}}
  {{/each}}
{{else}}
  {{yield}}
{{/unless}}
